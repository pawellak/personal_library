name: personal_library
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  #SMS
  telephony:

  #Bloc
  bloc:
  flutter_bloc:

  #Di
  injectable:
  get_it:

dev_dependencies:
  flutter_test:
    sdk: flutter

  #Di
  injectable_generator:
  build_runner:

flutter:
  uses-material-design: true